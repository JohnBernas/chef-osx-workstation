#
# RUBY
#

#
# rbenv
#
function rb;	rbenv version; end;

function rbl
  if test "$argv"
    rbenv local $argv
  else
    rbenv versions
  end
end

function rbi
  if test "$argv"
    rbenv install $argv
  else
    rbenv install -l
  end
end